version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Instalando dependências do node"
      - npm ci

  build:
    commands:
      - echo "Compilando TypeScript"
      - npm run build

      - echo "Criando .env"
      - |
        cat <<EOF > .env
        DATABASE_URL=postgres://boamassauser:admin@localhost:5432/boamassa
        JWT_SECRET=3a3253a19ee38b154d26d036db1a4b52
        PORT=8080
        EOF

  post_build:
    commands:
      - echo "Gerando artefato deploy.zip"
      - zip -r deploy.zip Dockerfile docker-compose.yml prisma src package*.json .env .ebextensions

artifacts:
  files:
    - deploy.zip
